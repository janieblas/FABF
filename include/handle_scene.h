#ifndef HANDLE_SCENE_H
#define HANDLE_SCENE_H

#include <SDL2/SDL.h>
#include "general_tools.h"

void handleScene(SDL_Renderer *renderer, GameState *state, ColorType colorType);

#endif //HANDLE_SCENE_H